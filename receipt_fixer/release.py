"""Brand constants for Receipt Fixer.

Matches the FixDrawer convention established by Seller CSV Fixer:
the same three constants drive window titles, About dialogs, and
PyInstaller metadata. Bump APP_VERSION on release.
"""

APP_NAME = "Receipt Fixer"
APP_BRAND = "FixDrawer"
APP_VERSION = "0.1.0"
